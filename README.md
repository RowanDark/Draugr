# Draugr

Web crawler and secret scanner — Go port of Draugr v2.0.

## Usage

```
draugr scan --url <target> [flags]
```

Run `draugr scan --help` for the full flag reference.

## Status

Scaffold only. Crawler implementation begins in Issue #2.
